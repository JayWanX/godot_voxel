<?xml version="1.0" encoding="UTF-8" ?>
<class name="VoxelBlockyModelMesh" inherits="VoxelBlockyModel" api_type="core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../doc/class.xsd">
	<brief_description>
		基于自定义网格生成模型。
	</brief_description>
	<description>
		[VoxelMesherBlocky] 不要求模型必须是立方体。归根结底，模型的视觉效果都是网格。这是制作模型最灵活的选择。工作流程是在 Blender 等 3D 编辑器中制作这些模型，确保它们被限制在从 (0,0) 到 (1,1) 的盒子内。纹理通过经典的 UV 映射来分配。
	</description>
	<tutorials>
	</tutorials>
	<members>
		<member name="mesh" type="Mesh" setter="set_mesh" getter="get_mesh">
			将用于此模型的网格。不能为空（如果你想要空模型，请使用 [VoxelBlockyModelEmpty]）。如果它有超过 2 个表面（2 个材质），后续的表面将被忽略。表面必须由三角形组成。必须是索引网格（如果你使用 [SurfaceTool] 生成网格，请使用 [method SurfaceTool.index]）。理想情况下，几何体应包含在 0..1 的区域内。只有位于该立方体区域各面上的三角形才会被考虑用于相邻面的剔除（另见 [member side_vertex_tolerance]）。
		</member>
		<member name="mesh_ortho_rotation_index" type="int" setter="set_mesh_ortho_rotation_index" getter="get_mesh_ortho_rotation_index" default="0">
			烘焙时应用到网格的正交旋转。取值遵循与 [GridMap] 瓦片相同的约定。
			（[GridMap] 提供了从 [Basis] 转换的方法，但遗憾的是它不是一个静态方法，因此需要存在一个 [GridMap] 实例。如果将来有需求，可以添加一个辅助方法。）
		</member>
		<member name="side_cutout_enabled" type="bool" setter="set_side_cutout_enabled" getter="is_side_cutout_enabled" default="false">
			当相邻体素部分覆盖此体素的一面时，该面被遮挡的几何体将被切除。这仅在两面的形状均为矩形时有效。注意，启用此选项实际上可能会产生更多三角形。
		</member>
		<member name="side_vertex_tolerance" type="float" setter="set_side_vertex_tolerance" getter="get_side_vertex_tolerance" default="0.001">
			低于此边距时，位于体素 6 个面之一附近的三角形将被视为位于该面。通过比较相邻面的三角形来决定各面是否被剔除。
		</member>
	</members>
</class>
