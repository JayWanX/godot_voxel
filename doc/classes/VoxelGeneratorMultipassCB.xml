<?xml version="1.0" encoding="UTF-8" ?>
<class name="VoxelGeneratorMultipassCB" inherits="VoxelGenerator" api_type="core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../doc/class.xsd">
	<brief_description>
		基于脚本、按数据块列和多遍处理工作的生成器。
	</brief_description>
	<description>
		此生成器可以用脚本实现，以数据块列的形式生成地形，而不仅仅是一个区块接一个区块地生成。
		它允许使用多遍处理，每一遍都可以访问之前各遍的结果，并允许访问相邻列。
		列的高度不是无限的，但可以通过每区块单遍的兜底方式定义上方和下方生成什么。
		它只能与 [VoxelTerrain] 一起使用。
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="_generate_block_fallback" qualifiers="virtual">
			<return type="void" />
			<param index="0" name="out_buffer" type="VoxelBuffer" />
			<param index="1" name="origin_in_voxels" type="Vector3i" />
			<description>
				为基于列的区域上方或下方的每个区块调用。例如，你可以决定在上方生成空气，在下方生成基岩。
			</description>
		</method>
		<method name="_generate_pass" qualifiers="virtual">
			<return type="void" />
			<param index="0" name="voxel_tool" type="VoxelToolMultipassGenerator" />
			<param index="1" name="pass_index" type="int" />
			<description>
				对每个数据块列，每一遍调用一次。
				必须使用传入的 [code]voxel_tool[/code] 获取要生成区域的信息，并用体素填充/编辑该区域。重要：不要将该对象保存在成员变量中以便后续复用。你只能在本次调用此方法时使用它。
				你可以使用 [code]pass_index[/code] 在每一遍中执行不同的操作。例如，0 可以是使用 Perlin 噪声的基础地面，1 可以种植树木和其他结构。
			</description>
		</method>
		<method name="_get_used_channels_mask" qualifiers="virtual const">
			<return type="int" />
			<description>
				使用此方法指示你的生成器将使用哪些通道。它返回一个位掩码，例如你可以提供这样的信息：[code](1 &lt;&lt; channel1) | (1 &lt;&lt; channel2)[/code]
			</description>
		</method>
		<method name="debug_generate_test_column">
			<return type="VoxelBuffer[]" />
			<param index="0" name="column_position_blocks" type="Vector2i" />
			<description>
				测试方法，将完整生成特定列的所有区块并返回它们。
				此函数不使用任何线程，也不使用内部缓存，因此会非常慢。不过，它可以让你更轻松地测试或调试脚本，例如使用一个独立的场景。
			</description>
		</method>
		<method name="get_pass_extent_blocks" qualifiers="const">
			<return type="int" />
			<param index="0" name="pass_index" type="int" />
			<description>
				获取某一遍可以访问其周围多少个区块（注意：默认情况下一个区块为 16x16x16 个体素）。
			</description>
		</method>
		<method name="set_pass_extent_blocks">
			<return type="void" />
			<param index="0" name="pass_index" type="int" />
			<param index="1" name="extent" type="int" />
			<description>
				设置某一遍在列生成时可以访问其周围多少个区块（注意：默认情况下一个区块为 16x16x16 个体素）。
				按设计，第一遍不允许访问相邻区块，因此它将保持为 0。
				后续各遍设计为至少可以访问 1 个区块的距离。此类遍不支持 0，因为那等同于将你的逻辑直接放在上一遍中。
				增加范围也会增加生成器的开销，无论是内存还是处理时间，因此应谨慎权衡。
			</description>
		</method>
	</methods>
	<members>
		<member name="column_base_y_blocks" type="int" setter="set_column_base_y_blocks" getter="get_column_base_y_blocks" default="-4">
			列的最低高度，以区块为单位。
		</member>
		<member name="column_height_blocks" type="int" setter="set_column_height_blocks" getter="get_column_height_blocks" default="8">
			列的高度，以区块为单位。
		</member>
		<member name="pass_count" type="int" setter="set_pass_count" getter="get_pass_count" default="1">
			列在被视为完全生成之前需要经历的遍数。更多的遍数会增加内存和处理开销。
		</member>
	</members>
	<constants>
		<constant name="MAX_PASSES" value="4">
		</constant>
		<constant name="MAX_PASS_EXTENT" value="2">
		</constant>
	</constants>
</class>
