<?xml version="1.0" encoding="UTF-8" ?>
<class name="VoxelStreamSQLite" inherits="VoxelStream" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../doc/class.xsd">
	<brief_description>
		将体素数据保存到单个 SQLite 数据库文件中。
	</brief_description>
	<description>
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="is_key_cache_enabled" qualifiers="const">
			<return type="bool" />
			<description>
			键缓存是否启用。
		</description>
		</method>
		<method name="set_key_cache_enabled">
			<return type="void" />
			<param index="0" name="enabled" type="bool" />
			<description>
				启用数据库键的缓存，以加快在只保存稀疏编辑数据块的地形中的加载查询。如果你的地形保存了其所有数据块（例如保存了生成器的输出），则这不会带来任何好处。
				必须在任何对 [code]load_voxel_block[/code] 的调用之前（即在地形开始使用它之前）调用此方法，否则将无法正常工作。你可以使用脚本来完成此操作。
			</description>
		</method>
	</methods>
	<members>
		<member name="database_path" type="String" setter="set_database_path" getter="get_database_path" default="&quot;&quot;">
			数据库文件的路径。[code]res://[/code] 和 [code]user://[/code] 应该可用，但 [code]res://[/code] 在导出后将无法工作（原因参见 [url=https://docs.godotengine.org/en/stable/tutorials/io/data_paths.html#accessing-persistent-user-data-user]此处[/url]）。路径可以相对于游戏的可执行文件。路径中的目录必须存在。如果文件不存在，它将被创建。
		</member>
		<member name="preferred_coordinate_format" type="int" setter="set_preferred_coordinate_format" getter="get_preferred_coordinate_format" enum="VoxelStreamSQLite.CoordinateFormat" default="2">
			设置创建新数据库时将使用的数据块坐标格式。这会影响支持的坐标范围以及 SQLite 执行查询的速度（影响较小）。打开现有数据库时，此设置将被忽略，而使用数据库本身的格式。目前无法更改现有数据库的格式，可能需要使用脚本从某个数据流加载各个数据块并将其保存到新的数据库中。
		</member>
	</members>
	<constants>
		<constant name="COORDINATE_FORMAT_INT64_X16_Y16_Z16_L16" value="0" enum="CoordinateFormat">
			坐标存储在 64 位整数键中，其中 X、Y、Z 和 LOD 为 16 位有符号整数。
		</constant>
		<constant name="COORDINATE_FORMAT_INT64_X19_Y19_Z19_L7" value="1" enum="CoordinateFormat">
			坐标存储在 64 位整数键中，其中 X、Y 和 Z 为 19 位有符号整数，LOD 为 7 位无符号整数。
		</constant>
		<constant name="COORDINATE_FORMAT_STRING_CSD" value="2" enum="CoordinateFormat">
			坐标存储在由逗号分隔的十进制数字字符串中，格式为“X,Y,Z,LOD”。
		</constant>
		<constant name="COORDINATE_FORMAT_BLOB80_X25_Y25_Z25_L5" value="3" enum="CoordinateFormat">
			坐标存储在 80 位二进制大对象中，其中 X、Y 和 Z 为 25 位有符号整数，LOD 为 5 位无符号整数。
		</constant>
		<constant name="COORDINATE_FORMAT_COUNT" value="4" enum="CoordinateFormat">
		</constant>
	</constants>
</class>
