<?xml version="1.0" encoding="UTF-8" ?>
<class name="VoxelMesher" inherits="Resource" api_type="core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../class.xsd">
	<brief_description>
		所有网格生成算法的基类。
	</brief_description>
	<description>
		为了让 Godot 能够渲染体素，可以将体素转换为网格。实现这一目标有多种方式，因此此类只是其他专用类的基类。体素节点会自动使用网格生成器，但你也可以手动生成网格。为此，你可以使用其中一个派生类。网格生成器可以被重复使用，这通常可以通过减少内存分配来获得更好的性能。
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="build_mesh">
			<return type="Mesh" />
			<param index="0" name="voxel_buffer" type="VoxelBuffer" />
			<param index="1" name="materials" type="Material[]" />
			<param index="2" name="additional_data" type="Dictionary" default="{}" />
			<description>
				根据提供的体素构建网格。材质将根据提供的数组附加到每个表面。材质的使用方式可能取决于网格生成器的类型。
				网格生成器最初是为数据块设计的，因此缓冲区外侧边距内的体素不会成为结果的一部分。它们被视为“相邻体素”，可能最终会影响面剔除。如果你想使用网格生成器制作独立的体素网格，请确保其周围用空气填充。该边距的大小由 [method get_minimum_padding] 和 [method get_maximum_padding] 确定。
			</description>
		</method>
		<method name="get_maximum_padding" qualifiers="const">
			<return type="int" />
			<description>
				获取网格生成器正常工作前，体素在其下角之前需要填充多少。
			</description>
		</method>
		<method name="get_minimum_padding" qualifiers="const">
			<return type="int" />
			<description>
				获取网格生成器正常工作前，体素在其上角之后需要填充多少。
			</description>
		</method>
	</methods>
</class>
